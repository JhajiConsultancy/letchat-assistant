# Sharecare Chat Widget — Environment Variables
# Copy this file to .env and fill in your values.

# Backend API base URL (leave empty to use the Vite dev proxy)
VITE_API_BASE_URL=

# Optional: static API key sent as "Authorization: Bearer <key>"
# Leave empty if the API is publicly accessible without a key.
VITE_API_KEY=
